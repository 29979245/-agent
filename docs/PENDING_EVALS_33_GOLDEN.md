# PENDING — 系统行为 Golden 数据集（33 号文档依赖项）

> 状态：**SKIPPED / 待 33 号文档补充后回补**
> 创建：2026-08-28
> 关联：54-Evals评测体系搭建.md（Step 2 依赖）、32-评测体系设计.md（设计源头）

## 为什么被跳过

54-Evals评测体系搭建.md 第八章工作流的 Step 2（`/grill-with-docs`）要求阅读：

- `../4.产品设计/32-评测体系设计.md` —— 评测金字塔方法论 ✅ 已有
- `../4.产品设计/33-端到端测试与Golden数据集设计.md` —— 系统行为 Golden 数据集架构 ❌ **未提供**

`33 号文档`定义**系统行为 Golden 数据集**（YAML 格式，Baseline/Boundary/Regression 三层），测"系统行为是否正确"（路由、工具调用顺序、工作流轨迹），对应确定性评测轨道。

本地 `4.产品设计/` 目录下 33 号实际是"家长端与通知系统设计"，课程序列号与本地产品设计目录编号不一致，33 号评测文档缺失。

**决定**：跳过与 33 号直接相关的任务，保留需求与占位说明，不擅自补充或修改缺失文档内容。跳过不影响 55-59 号 Agent 架构的 Explore 衔接。

## 已被设计文档固定的需求（非编造，来自 32 号原文）

这些内容是 32 号已明确定义的，**不作为缺失项**：

- **109 个场景完整定义**：32 号 §5.1 已给出全部 109 条（GW/SEC/TOOL/WF/EDGE/ERR/PLAN/STATE/RES/DEFECT/ROLE/SSE/PASS/PERF），含输入与预期输出/断言/评估标准。
- **组织方式**：32 号 §9.3 —— 按"层级 → 维度"两级分类存放（基线 60 / 边界 24 / 回归 25）。
- **场景字段结构**：32 号 §7 —— 三种结构（确定性 / LLM-as-Judge / 性能），确定性含 8 种断言类型，安全场景含拦截类型与误拦容忍度。
- **执行粒度**：32 号 §10.1 —— 支持按需只跑某几层或某几个维度（"只改了路由逻辑 → 只跑 Gateway 路由的 24 个场景"）。
- **双轨共享格式**：32 号 §决策四 —— 确定性轨道与 LLM-as-Judge 轨道共享相同场景定义格式、使用不同断言引擎。
- **CI 门禁**：32 号 §8 —— 安全 P0(100% 阻断) / 路由 P0(≥85% 阻断) / 质量 P1(≥3.5/5 告警) / 性能 P2(P95 告警)；双轨触发策略（确定性每次 commit，LLM 每次 PR）。
- **生命周期规则**：32 号 附录 B —— 层级升级规则（边界→基线、回归→边界）、场景退役、阈值调整触发条件。

## 占位说明（待 33 号文档确认，勿视为定稿）

探索过程中形成了一套**候选** YAML 文件组织方案。它是基于 32 号规则的推断，**不是 33 号文档的内容**，仅作为 33 号到位后的对比基线：

- 候选：`tests/evals/golden_system/*.yaml`，每评测维度一个文件（14 个文件），层级（baseline/boundary/regression）作为字段而非目录。
- 候选字段：`id`、`layer`、`dimension`、`track`（deterministic/llm_judge/performance）、`ci_gate`（p0_sec/p0_route/p1_quality/p2_perf）、`input`、`input_type`（forward/adversarial/boundary）、`expected`/`assertions` 或 `scoring_dimensions`。

**33 号文档补充后，须以其实际规格为准修正/替换本占位。**

## 回补清单（33 号文档到位后执行）

1. 读取 33 号文档，确认系统行为 Golden 的 YAML 文件组织、schema、字段定义。
2. 对比候选方案与 33 号规格，记录差异并修正。
3. 生成系统行为 Golden 数据（109 场景 → YAML）。
4. 实现加载器 + schema 校验（与 54 号内容质量 Golden 的加载器并列）。
5. 与 54 号双轨衔接：确定性轨道加载系统行为 Golden，LLM 轨道加载内容质量 Golden。
6. 运行验证 + 更新本文档状态为 DONE。

## 衔接说明

- 54 号 Evals 体系**内容质量部分**（Golden数据集.md：100 样本 / L1/L2/L3 / 容差参数 / 边界条件 / 回归基线）**不依赖 33 号**，可独立推进。
- 55-59 号 Agent 架构 Explore 已按计划继续，不受本次跳过影响。
